apiVersion: v1
kind: Service
metadata:
  name: {{ include "episense.fullname" . }}
  labels:
    {{- include "episense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "episense.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
